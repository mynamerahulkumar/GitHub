import { SalesPersonDetail } from './sales-person-detail';

describe('SalesPersonDetail', () => {
  it('should create an instance', () => {
    expect(new SalesPersonDetail()).toBeTruthy();
  });
});
